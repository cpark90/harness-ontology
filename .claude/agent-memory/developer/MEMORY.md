# developer 역할 메모리

온톨로지 노드 저작 에이전트의 역할 특화 메모리 인덱스. 역할 정의는
`.claude/agents/developer.md`. 노드 종류별 함정·모델링 패턴·capability 배선을 파일로 추가하고
아래에 **한 줄로** 인덱스한다(상세는 각 토픽 파일에).

- 완결 brief로 받은 배정분만 구현: 온톨로지 노드(`ontology/abox/`) 또는 배정 소스(`tools/**` 등).
  TBox·shapes·brief 밖 경로는 안 만짐. 검증(vnv)·커밋(inspection)은 안 함. 온톨로지 노드 스타일은
  `ONTOLOGYSTYLE.md` [지킴], 소스는 기존 컨벤션·언어 표준.
- 도구는 `rdflib` 등이 있는 인터프리터로(예: `/usr/bin/python3`). 셸 기본 python3엔 없을 수 있음.
- **반복 핵심**: (1) 중간노드(subject≠Harness) 도달성=hasComponent **propertyChain**(직접 sub면
  rdfs:domain Harness로 mistype); Harness→X 직결만 직접 subPropertyOf hasComponent. (2) 한 술어가
  2클래스 공용이면 rdfs:domain **생략**+definition 명시(prp-dom mistype 회피, owl:unionOf repo 0회).
  (3) ⊑HarnessComponent 개체는 SHACL ComponentConnectivityShape→반드시 harness 배선/rollup(concept
  tag만으론 부족). (4) 기존 하네스 bound 노드의 prefLabel/definition 수정=그 CLAUDE.md 변함(byte-id 깨짐);
  materialize emitter 안 읽는 술어 추가는 CLAUDE.md 불변→2run cmp로 증명(git無). (5) recipe closure는
  HARNESS_ROOT_ONTOLOGY=recipe **IRI**(디렉토리경로X)로 로드해야 recipe-local 포함.

<!-- 학습 인덱스 (한 줄씩) -->
- [execution-mode-first-class-axis](execution-mode-first-class-axis.md) — 실행 topology를 `appliesPattern∩tag`에서 **1급 속성**으로 승격: `ho:ExecutionMode`⊑SpecConcept(비-HC)+`hasExecutionMode`(NOT⊑hasComponent)+EdgeTyping `sh:class`. ★"확장성"=값 축을 **개체로 열거**(닫힌 `sh:in` 금지) → 새 모드=개체 1개, scratch overlay로 4번째 `mode-swarm`(외부 도메인)까지 PASS 증명. 비-HC 신규 leaf 체크리스트: SpecConcept definition의 leaf 열거 갱신 + **`lib.INSTANCE_CLASSES` 필수**(HC fallback 없음 → 미등록시 개체 카운트/reachability/retrieve에서 증발, 205 게이트가 이를 폭로) + INSTANCE_LINK_PREDICATES + ONTOLOGYSTYLE §2 행 + **옛 메커니즘을 서술한 stale prose grep**(as-execution-mode·c-execution-mode). 폐기=삭제 대신 maturity "deprecated"+superseded 문장, tagged 유지(deprecated도 reachability 면제 아님), 신규 unit 대신 같은 파일 co-locate. 문서영향=CLAUDE.md `## Execution mode` 순수 append(3하네스 +6/-0), MANIFEST byte-id(모드는 컴포넌트 아님), lock individualCount만 202→205. 모드 배정 근거는 채널/워크플로 사실로: sub-agents(chan-dispatch)·agent-teams(chan-peer)·hybrid(factory=dispatch 저작 vs task-board 검증/진화 phase), 근거는 definition 아닌 TTL 주석에(문서 diff 최소). ★**채널=전달매체 축 ≠ spawn topology 축**(직교) — chan-workspace가 중립이라고 모드 미결정이 아니며, 패턴 없는 하네스는 derivedFrom+hasWorkflow+role definition의 "dispatch-invoked only" 문구로 판정(h-workspace-synthesis→mode-sub-agents).
- [assembly-sections-run-behaviour-renderers](assembly-sections-run-behaviour-renderers.md) — GAP-4 run-behaviour 섹션 4(order 9~12)+materialize 렌더러. ★기본순서 보유자=h-multiagent이므로 신규 kind는 그 문서에 직결: execution-mode/error-handling/test-scenarios는 부품 없어 무출력, **data-flow는 wf-multiagent의 dlv-* DAG 때문에 emit→byte-id 충돌** ⇒ 미저작+order 10 공석으로 **에스컬레이션**(unbound=orphan FAIL이라 중립 선택지 없음) → orchestrator가 (a)수용 판정: **byte-id 불변식은 리팩터 보호용이지, 이미 그래프에 있는 데이터를 드러내는 기능을 막는 근거가 아니다**. 결과 CLAUDE.md는 `## Data flow` 순수 append(삭제줄 0), wf-multiagent 바인딩 recipe 전부 동일 획득. MANIFEST는 원리상 byte-id 불가(+4행, tokenEstimate 49794→49888). 렌더러=전부 조건부 early-return, execution-mode는 클래스 없이 `appliesPattern`∩`tagged c-execution-mode`(lib.EXECUTION_MODE_CONCEPT), data-flow=step produces→consumes join, error-handling=md표+`_cell()` 파이프 이스케이프. 증명=scratch overlay(카탈로그 절대경로 복사+자체 order harness, targetsDomain 필수). enum 확장 시 prose stale 3곳(TBox sectionKind/AssemblySection definition·host 배너) 동반 수정.
- [verification-unit-relocation](verification-unit-relocation.md) — GAP-3 `core/verification/` 신설=순수 relocation(scn- 2+fp- 2를 process/workflows.ttl→신규 유닛, 라인슬라이스 byte-fidelity, 개체 수 198 불변). federation 4점(root imports·root catalog·staging 공유 catalog·dedicated catalog). ★dedicated `catalog-<recipe>.xml`은 REORG 때 미갱신으로 평면경로+신규유닛 누락 → 에러 없이 **부분 closure(41 individuals)** 로 FAIL; central 블록을 공유 catalog 블록으로 통째 치환해 4개 복구(216~218 PASS). 유닛 신설 시 ONTOLOGYSTYLE §4·abox-taxonomy-reorg의 "verification/=파일 없는 그룹" 예시가 stale.
- [revfactory-p1-lifecycle-verify-abox](revfactory-p1-lifecycle-verify-abox.md) — P1 잔여 개체 13(wf-harness-evolution/wf-verify-harness+step 7, scn- 2, fp- 2) → 전부 `h-harness-factory`에 배선(메타 파트 host; h-multiagent 물리면 CLAUDE.md byte-id 깨짐 — stash 2run diff로 CLAUDE/MANIFEST 동일 증명, lock의 전역 individualCount만 185↔198). ★brief의 "land됨" 목록 불신: `ho:ExecutionMode`/`hasExecutionMode`/`stepFailurePolicy` TBox에 **없음**(D2 경량화)이고 축은 이미 pat-agent-teams/sub-agents/hybrid+c-execution-mode로 존재 → 저작=클래스발명+중복 ⇒ GAP만 보고. TestScenario=kind exactly1(closed 5)+prompt≥1, FailurePolicy=condition+strategy≥1; 둘 다 직접⊑hasComponent라 harness 배선만으로 orphan-free, INSTANCE_CLASSES 없어도 HC fallback(단 scenarioReferences는 INSTANCE_LINK 미등록·materialize 렌더러 없음=Wave C). `verification/` unit 부재→process/workflows.ttl에 배너 달고 co-locate(신규 unit=federation 3점, orchestrator 소관).
- [abox-da4-groupdir-reorg-recipe-sync](abox-da4-groupdir-reorg-recipe-sync.md) — REORG-2: recipe staging catalog를 REORG-1 그룹경로로 동기화. 13 core `central/ontology/abox/core/<group>/<type>.ttl`(root catalog와 동일 그룹, `central/` prefix만) + 신규4(observational/observation·state/memory·information-space·assembly). ★공유 catalog-v001.xml에 pilot 4개(03/16/31/46) 누락→dedicated catalog-<name>.xml에만 있었음→per-recipe closure(HARNESS_ROOT_ONTOLOGY=recipe IRI)로 검증하려면 공유에 8 recipe uri 전부 필요→consolidate. 5 pilot PASS(200~205 individuals). stale docs 3(federation-design 88/179/249·recipes-design 13·ci yaml 16/21)=평면경로→그룹경로 서술, 논리 IRI 위치독립 명시.
- [abox-da4-groupdir-reorg](abox-da4-groupdir-reorg.md) — REORG-1 중앙 ABox→DA-4 그룹 디렉토리(`core/<group>/<type>.ttl`) 순수 이동+grab-bag split. 논리 IRI `data/core/<type>` 위치독립→catalog uri경로만 갱신(tool 하드코딩 無, glob fallback도 `**` recursive라 OK). split=byte 라인슬라이스(prefix L1-7+신규 owl:Ontology 헤더+섹션). roles→roles+observation+memory, domains-tasks→+information-space, harnesses→+assembly-sections. federation 3점(catalog 13경로+4신규·root imports+4·ONTOLOGYSTYLE §4). validate 185 불변.
- [da4-intermediate-superclass-taxonomy](da4-intermediate-superclass-taxonomy.md) — DA-4 flat→중간계층 순수 TBox 재부모화(harness.ttl only): 22 HC leaf→9 중간(⊑HC), 7 비-comp leaf→2 top-level(SpecConcept/InformationSpace, NOT⊑HC). zero-risk=owlrl transitivity로 leaf 인스턴스 여전히 HC/Capability 타입 추론→shape/reachability/count 무영향, 중간은 INSTANCE_CLASSES 불요(직접 인스턴스0), class-def 미렌더→materialize byte-id 자동. 비-comp leaf는 subClassOf 신설(a owl:Class↔rdfs:label 사이 삽입). 검증: validate 185 불변·h-multiagent byte-id·recipe21 closure PASS. 단일상속.
- [da2-definition-disambiguation](da2-definition-disambiguation.md) — DA-2 정의명확화(B~F): skos:definition만 편집=그래프 구조 무변경 zero-risk(validate 185 불변, class-def는 CLAUDE.md 미렌더→byte-id 자동). 혼동쌍 de-conflate 패턴=자기지칭/교차어 제거 + "Distinguished from ho:X" 절. Workflow=흐름형태 서술(패턴명 금지)/DesignPattern=appliesPattern 태그. ★brief가 없는 prop 인용(ho:notation)→실제(integrationMode/triggerPhrase) grep확인 후 사용. DA-1잔재 cleanup=domains-tasks 3곳+ONTOLOGYSTYLE 명명표 oa-1행→os-/aoi-/oa- 3행.
- [da1-observation-tripartite-split](da1-observation-tripartite-split.md) — DA-1 원자 refactor: `ho:ObservationArea`→3클래스(ObservationSpace Ω_i=CAN-observe·AreaOfInterest=intent·AreaOfObservation=realized). agentObservation→Space; NEW hasAreaOfInterest/hasAreaOfObservation(중간노드,NOT⊑hasComponent)/coversInterest; 도달성=hasComponent 7th/8th 3-link chain. observationKind(AoI+AoO)·unobserved(Space+AoO)=domain생략. ABox os-/aoi- 각5신설+oa- 10 retype(projectsFrom→os,external coversInterest). byte-id(관측 emitter 0). validate PASS 185/recipe200. FLAG=domains-tasks.ttl(경계밖) stale prose+ONTOLOGYSTYLE os-/aoi-.
- [mas-wave3b-infospace-abox](mas-wave3b-infospace-abox.md) — MAS W3b ABox: 정보공간 투영사슬 개체. +2 singleton(env-space·global-state, Domain-form 비-HC=shape/tokenEstimate無) + dom-design scopedFrom + oa projectsFrom + h-multiagent hasGlobalState. orphan회피=WEAK-CONNECTIVITY(4투영술어 INSTANCE_LINK 등록, 방향무관)→host harness 불요. validate PASS 175, recipe 190.
- [mas-wave3a-infospace-tbox](mas-wave3a-infospace-tbox.md) — MAS W3a TBox: 클래스2 비-HC(EnvironmentSpace·GlobalState, Domain꼴 subClassOf無) + 투영속성4 NONE⊑hasComponent(scopedFrom/describesDomain/hasGlobalState/projectsFrom). hasGlobalState=Harness→X지만 ⊑hasComponent 금지(GlobalState 비-HC mistype). GlobalStateShape, Env shape-free. validate PASS 173 INVARIANT.
- [mas-wave2-agent-observationarea-abox](mas-wave2-agent-observationarea-abox.md) — MAS W2 ABox: agent 5(agentRole REUSE·agentFunction 기존cap SOFT·cognitiveCapacity 150000) + oa 10. anti-orphan=6th hasComponent chain→h-multiagent에 hasAgent만. byte-id(hasAgent emitter無). recipe 188, validate 173.
- [mas-wave1-agent-observationarea-tbox](mas-wave1-agent-observationarea-tbox.md) — MAS W1 TBox: `ho:Agent`·`ho:ObservationArea`⊑HC + hasAgent(직접 sub) + agent/observation props(NOT sub). ObservationArea orphan방지=6th propertyChain `(hasComponent agentObservation)`. area크기=tokenEstimate 재사용. AgentShape/ObservationAreaShape. validate 158 INVARIANT.
- [revfactory-wave-b1-coordination-governance](revfactory-wave-b1-coordination-governance.md) — coord/governance: concepts+4·patterns+9(pat-supervisor DYNAMIC vs orchestrator-workers STATIC)·chan-task-board·guardrails+13. ★Channel/Guardrail(⊑HC)는 concept tag만으론 orphan(ComponentConnectivityShape)→hasChannel/hasGuardrail 배선 필수=전용 중립 host harness(NOT h-multiagent).
- [revfactory-tbox-wave-a](revfactory-tbox-wave-a.md) — 방법론 TBox: 클래스2(TestScenario/FailurePolicy⊑HC)+직접sub 2(hasTestScenario/hasFailurePolicy)+refinement edge 2(augmentsRole·scenarioReferences)+datatype. Harness+Instruction 공용 datatype=domain생략. validate 130 INVARIANT.
- [agent-memory-tier-model](agent-memory-tier-model.md) — firmware/cache/long-term 3-tier=`ho:Memory`⊑HC+`hasMemory`⊑hasComponent(직접sub); 구분 4 discriminator(closed set sh:in). 기존 unit additive. materialize INSTANCE_CLASSES 없어 HC fallback(CLAUDE byte-id). validate 130(+4).
- [corpus-persist-local-path-canonical-source](corpus-persist-local-path-canonical-source.md) — 코퍼스 안정 로컬경로 영속화+5 recipe repoint. artifactTemplate=fetch LOCAL abs path vs dct:source=canonical URL. materialize 0 stub+2run IDENTICAL.
- [recipe-ml-experiment-newdomain-pilot](recipe-ml-experiment-newdomain-pilot.md) — recipe 31-ml-experiment: NEW local dom-ml+task. Concept tree 4(local TOP topConceptOf). ★persona artifactTemplate absent→materialize HARD-FAIL(skill은 .ref stub). dedicated catalog+--out. validate 146.
- [recipe-fullstack-webapp-toolscope-variation](recipe-fullstack-webapp-toolscope-variation.md) — 16-fullstack: qa-engineer LOCAL role(worker+gate 하이브리드→synthesizer 재사용 부적합, 로컬+providesCapability cap-synthesis). RULE: 순수 gate만 synthesizer. Tool-scope 변이=role별 roleTool slice. persona ref absent=HARD-RAISE(BLOCKING).
- [recipe-authoring-code-reviewer-pilot](recipe-authoring-code-reviewer-pilot.md) — recipe 21: worker persona=INLINE promptText+full body artifactTemplate. QA gate=core:role-synthesizer 바인딩. staging catalog uri 상대→central 심링크+cd. validate 141.
- [central-library-growth-host-harness](central-library-growth-host-harness.md) — 재발 중립파트 promote-once. anti-orphan=h-multiagent 배선금지(byte-id)→전용 host `h-workspace-synthesis`. cap satisfaction via Role(host requires+role provides, hasRole rollup). 새 파일 0=loader parity 자동.
- [task-dag-and-coordination-topology](task-dag-and-coordination-topology.md) — GAP-1 `ho:Deliverable`⊑HC+stepProduces/Consumes/DependsOn: 도달성=5th 3-link chain. DAG는 MANIFEST-only(CLAUDE byte-id). topology=Pattern+Channel 쌍(pat-peer-mesh+chan-peer, 중립 host h-peer-mesh). ★bound 노드 def 수정=CLAUDE 변함(revert).
- [assembly-order-graph-driven](assembly-order-graph-driven.md) — CLAUDE 섹션순서 그래프화: `ho:AssemblySection`⊑HC+assemblyOrder+sectionKind(closed). hasAssemblySection=Harness→X→직접 subPropertyOf(propertyChain 아님). 기본 8 as-*→h-multiagent DEFAULT. materialize SECTION_RENDERERS+resolve_assembly_order.
- [systemprompt-section-decomposition](systemprompt-section-decomposition.md) — SystemPrompt→`ho:PromptSection`⊑HC: hasSection(plain)+sectionOrder. 도달성=4th chain `(hasComponent hasSection)` 2-link. materialize `_prompt_sections`(정렬 조인), section-less blob 유지.
- [workflow-step-decomposition](workflow-step-decomposition.md) — Workflow→`ho:WorkflowStep`⊑HC: hasStep(plain)+stepOrder+stepUsesTool/ByRole/GuardedBy. 도달성=3rd chain `(hasComponent hasStep)`. materialize `_workflow_steps` 중첩 emit.
- [recipe-product-manager-pilot](recipe-product-manager-pilot.md) — recipe 46: NEW dom-product+LOCAL Task. least-privilege tool-editor ONLY(DROP cap-codeexec, HarnessShape 안요구→PASS). persona artifactTemplate absent HARD-FAIL. validate 146.
- [recipe-newsletter-engine-content-domain](recipe-newsletter-engine-content-domain.md) — recipe 03: QA gate 2 review agent(convergence→synthesizer REUSE, PRODUCING stage→LOCAL). tool scope가 harness cap set 결정(NO shell→no cap-codeexec). NEW dom-content. validate 146.
- [recipe-references-not-stored-artifacts](recipe-references-not-stored-artifacts.md) — recipe=parts+methodology+references+README, concrete build문서 저장금지(vendoring 안티패턴). materialize FETCH(`_resolve_ref_path`+`_ref_stub` impl/scaffold/skill 대칭). ODR INV-1.
- [odr-contract-verify](odr-contract-verify.md) — ODR VERIFY: capability에 `ho:Contract`+capabilityContract+contractKind/Check, 도달성=3-link chain, verify_contract.py dual.
- [materialize-atomic-emit-closed-policy](materialize-atomic-emit-closed-policy.md) — materialize 하드닝: atomic emit(`_place_atomically`), closed policy set(미인식 raise).
- [instruction-skill-emitter](instruction-skill-emitter.md) — Claude SKILL=`ho:Instruction`⊑HC+hasInstruction⊑hasComponent(스키마 무수정). recipe LOCAL. emitter=`## Skills`+MANIFEST.
- [materialize-channel-emitter](materialize-channel-emitter.md) — ho:Channel EMIT: `channel_record()` helper, if-channels 가드, 중앙불변.
- [methodology-as-nodes](methodology-as-nodes.md) — 산문절차→Workflow+DesignPattern+Guardrail+Concept×N(broader), 전부 h-multiagent 배선.
- [glossary-term-layer](glossary-term-layer.md) — 거버넌스원칙=독립 skos:Concept(`ho:Term` 발명금지). topConceptOf는 연결 안침→top은 자식 skos:broader로. dup=(class,label) advisory.
- [recipe-inherits-shared-parts-by-iri](recipe-inherits-shared-parts-by-iri.md) — derivedFrom=lineage뿐(컴포넌트 상속 아님)→충실반영은 명시 edge. 공유 중립파트 로컬저작 금지, core: IRI REUSE.
- [robust-recipe-import-closure](robust-recipe-import-closure.md) — recipe는 중앙 root 온톨로지 하나만 owl:imports→새 core유닛 자동전파(bare IRI SHACL FAIL 회피). catalog=root+전 core.
- [coverage-gap-prevention](coverage-gap-prevention.md) — coverage 갭방지: gr-structural-coverage + CLAUDE step7 audit gate. 어휘 없는 소스요소=schema EXTEND 신호(silent skip 금지).
- [channel-coordination-core-unit](channel-coordination-core-unit.md) — 채널=`ho:Channel`⊑HC 개체, 새 core unit channels.ttl(catalog+root 3점). hasChannel⊑hasComponent·channelParticipant·involvesUser·channelMedium.
- [role-taxonomy-new-core-unit](role-taxonomy-new-core-unit.md) — 역할분류=`ho:Role` 개체+새 core unit roles.ttl(catalog+root 둘다→로더 parity). userFacing bool. hasRole⊑hasComponent.
- [role-characteristics-optional-userfacing](role-characteristics-optional-userfacing.md) — role 특성명시(roleGuardrail/Tool/MemoryPolicy REUSE). userFacing optional=present-only(true만). roleTool 새 tool→harness usesTool에도.
- [faithful-source-reflection](faithful-source-reflection.md) — 실소스 FAITHFUL 반영: 합성데모 제거, BIND후보 단일→implementationRef collapse, role=실 .claude/agents 1:1.
- [odr-bind-lock-candidates](odr-bind-lock-candidates.md) — ODR BIND+Lock: `ho:Candidate`⊑HC+selectionPolicy+Version/Tag. implementationCandidate=property chain rollup(직접sub면 Tool mistype). harness.lock.json(sha256).
- [materialize-roles-impl-scaffold](materialize-roles-impl-scaffold.md) — materialize 증분2: P4 Role emit(.claude/agents), P3 implementationRef byte-copy(→.ref stub), P5 scaffold(scaffold/ marker 후 mirror).
- [materialize-build-projection](materialize-build-projection.md) — `materialize.py`=retrieve DUAL(harness→CLAUDE.md+MANIFEST.json). validate 게이트 후 build, 결정성. P2 artifactTemplate({{}} 치환·absent⇒graph fallback).
- [neutral-parts-decomposition](neutral-parts-decomposition.md) — 온톨로지=domain-INDEPENDENT PART 라이브러리: governance doc→중립 Guardrail/Pattern/Workflow/SystemPrompt, 도메인명사 제거, h-multiagent 배선.
- [model-external-harness](model-external-harness.md) — 외부 하네스 abox: role→Workflow+pattern, tool→Tool+cap, CLAUDE규칙→Guardrail, requires↔provides 짝, 개념 reachability(ho:tagged 필수).
- [webui-svelte-frontend](webui-svelte-frontend.md) — tools/webui Svelte+Vite: vite outDir=../static, compose ./tools 마운트 제거, Dockerfile 멀티스테이지, /api/*.
- [split-core-per-type-units](split-core-per-type-units.md) — core seed.ttl→타입별 다중문서(abox/core/<type>.ttl): byte-identical 이동, root union wiring(각 유닛 schema만 import).
- [recipe-repo-composition](recipe-repo-composition.md) — 중립 core에서 Harness 조립 recipe repo=assembly spec(core owl:imports+IRI 참조, 도메인만 LOCAL). 검증=env override 중앙 validate.py.
- [guardrail-item-datatype-property](guardrail-item-datatype-property.md) — 규칙세부=별도노드 아닌 datatype property 1개(`ho:languageCondition` domain Guardrail)+ABox 다중 리터럴. sh:closed 없으면 shapes 무수정.
- [federation-owl-imports-catalog](federation-owl-imports-catalog.md) — GitHub 연합: glob→owl:imports+catalog 로더(glob fallback·env override), IRI `.../id/<domain>/<slug>`. Write 툴 stray `</content>` gotcha.
- [ontologystyle-naming-table-audit](ontologystyle-naming-table-audit.md) — ONTOLOGYSTYLE §2 접두사표 실측 감사: 근거는 참조 아닌 **선언**(`^id:x- a ho:X`) grep, 기존 행 예시도 전수 존재확인(허구 3건 발견), core 0-인스턴스 클래스(ins-/contract-/cand-)는 staging recipe에만 실재→표 유지+recipe IRI 예시, DA-4 중간 superclass는 행 없음(구체 클래스만). 배치=부모 뒤 자식.
